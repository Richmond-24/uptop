
module.exports = {
  darkMode: 'class', // required for class-based toggling
  content: ['./app/**/*.{ts,tsx}', './components/**/*.{ts,tsx}'],
  theme: {
    extend: {
      // Optional: for animations like fadeIn, fadeInUp
    },
  },
  plugins: [],
}
